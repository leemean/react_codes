const firstNumber = 1;
const secondNumber = 2;

const total = firstNumber + secondNumber
const total2 = secondNumber - firstNumber

function getTotal2(first:number,second:number){
    return first + second
}

const total3 = getTotal2(1,2)
